[**@arrai-innovations/reactive-helpers**](../README.md)

***

[@arrai-innovations/reactive-helpers](../README.md) / utils/proxyRunning

# utils/proxyRunning

## Functions

### proxyRunning()

> **proxyRunning**(`parentState`, `parentStateProp`, `ref`): `void`

Proxy a parent's running state to a child's running state.

#### Parameters

##### parentState

`any`

The parent state.

##### parentStateProp

`string`

The parent state property.

##### ref

`Ref`\<`boolean`, `boolean`\>

The ref to proxy to.

#### Returns

`void`
